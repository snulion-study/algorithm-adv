#include <iostream>
#include <vector>

using namespace std;
int main(){
  ios::sync_with_stdio(0), cin.tie(0);

  int T;
  cin >> T;
  int *result = new int[T];

  for (int t = 0; t<T; t++){
    int N;
    cin >> N;

    vector<vector<int>> a;
    for (int n = 0; n<N ; n++){
      
    }
  }



  return 0;
}